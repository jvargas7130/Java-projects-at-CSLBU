import java.awt.Color;
import java.awt.Graphics;
import java.awt.Point;
import java.awt.Rectangle;

/**
 * 
 * @author j**
 * This class represents each of the cities the defender must protect.
 *Panel extends rectangle to use its location and collision detection methods
 * 
 * @author jvarg
 *
 */

public class City extends Rectangle {
	boolean active;
	Color color;
	
	public  City(Point loc, Color c){
		
		color = c;
	}

	public void draw(Graphics g, int height ){
		g.setColor(color);
		
		g.drawRect(0, height - 220, 100, 220);//building
		g.drawRect(200,  height - 250, 130,250 );//building
		
		g.setColor(Color.RED);
		g.drawRect(400,  height - 100, 70,100 );//battery
		
		g.drawRect(600, height - 280, 130,280);//building
		g.drawRect(800,height  - 220, 100,220 );//building
		
		g.drawRect(1000, height  - 100, 70,100 );//battery
		
		g.drawRect(1200,height  - 280, 100,280 );//building
		g.drawRect(1400, height  - 250, 130,250 );//building
		g.drawRect(1600, height - 150, 130,150 );//building
		
		g.drawRect(1800,  height  - 100, 70,100);//battery
		
		
	}
	
	public boolean isActive(){
		return true;
	}
	
	public Point getLocPoint(){
		return null;
		
	}
	
	
	
}
